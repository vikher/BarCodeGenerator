<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:WaterwayBarCodeGenerator"
             x:Class="WaterwayBarCodeGenerator.LoginPage">

    <StackLayout HorizontalOptions="Center" VerticalOptions="Center"  Margin="10,10">
        <Image x:Name="wwlogo" VerticalOptions="Fill" Aspect="AspectFill"/>
        <ActivityIndicator x:Name="ActivityIndicator" IsRunning="False" IsEnabled="False"  
                       IsVisible="False" VerticalOptions="Center" HorizontalOptions="Center" Color="Blue"/>
        <Button x:Name="loginButton" Text="Login" Clicked="OnLoginButtonClicked" />
    </StackLayout>

</ContentPage>